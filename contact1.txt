<!DOCTYPE html>
<head>
<meta charset="UTF-8">
    <title>Contact Us - SVARA </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'DM Sans', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background: #F7F5F2;
        }

        .header {
            background-color: #121212;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            flex-wrap: wrap;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 8px rgba(0,0,0,0.3);
        }
        
        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 900;
            letter-spacing: 2px;
        }

        .nav {
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }

        .nav a {
            color: rgba(255,255,255,0.85);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            transition: color 0.2s, background 0.2s;
            padding: 0.42rem 0.78rem;
            border-radius: 999px;
        }

        .nav a:hover,
        .nav a.active {
            color: white;
            background: rgba(255,255,255,0.14);
        }

        .mobile-menu {
            display: none;
            background: none;
            border: 2px solid white;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 5px;
        }

        .footer {
            background: #121212;
            color: white;
            text-align: center;
            padding: 2rem 1rem;
            margin-top: 2rem;
            font-size: 0.9rem;
        }

        .footer-links {
            margin-bottom: 1rem;
        }

        .contact-section {
            padding: 2rem 1rem;
            max-width: 1200px;
            margin: 2rem auto;
        }

        .section-title {
            text-align: center;
            margin-bottom: 0.9rem;
            font-size: 2rem;
            color: #121212;
            font-family: 'Playfair Display', serif;
        }

        .section-intro {
            text-align: center;
            color: #5f6b76;
            max-width: 720px;
            margin: 0 auto 2rem;
        }

        .contact-content {
            background: #FFFFFF;
            border-radius: 16px;
            box-shadow: 0 6px 22px rgba(0,0,0,0.13);
            border: 2px solid #C8C0B5;
            padding: 1.8rem;
            display: flex;
            flex-direction: column; 
            gap: 2rem;
        }

        .contact-form,
        .contact-info {
            background: rgba(255,255,255,0.82);
            border: 2px solid #C8C0B5;
            border-radius: 14px;
            padding: 1.35rem;
            box-shadow: 0 6px 18px rgba(18,18,18,0.06);
        }

        .contact-form h3,
        .contact-form label {
            display: block;
        }

        .contact-form h3,
        .contact-info h3 {
            margin-bottom: 1rem;
            color: #121212;
            font-family: 'Playfair Display', serif;
            font-size: 1.35rem;
        }

        .contact-form label {
            margin-bottom: 0.5rem;
            font-weight: 700;
            color: #4b5563;
        }

        .contact-form input[type="text"],
        .contact-form input[type="email"],
        .contact-form textarea {
            width: 100%;
            padding: 0.85rem 0.95rem;
            margin-bottom: 1rem;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            font-size: 0.95rem;
            font-family: 'DM Sans', Arial, sans-serif;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }

        .contact-form input[type="text"]:focus,
        .contact-form input[type="email"]:focus,
        .contact-form textarea:focus {
            outline: none;
            border-color: #121212;
            box-shadow: 0 0 0 3px rgba(44, 62, 80, 0.08);
        }

        .contact-form textarea {
            resize: vertical;
            min-height: 150px;
        }

        .submit-button {
            background: #1A1A1A; 
            color: white;
            padding: 0.82rem 1.4rem;
            border: none;
            border-radius: 6px;
            font-size: 0.95rem;
            cursor: pointer;
            width: 100%;
            font-family: 'DM Sans', Arial, sans-serif;
            font-weight: 700;
            transition: background 0.2s ease, transform 0.15s ease;
        }

        .submit-button:hover {
            background: #3A3A3A;
            transform: translateY(-1px);
        }

        .info-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 1rem;
            gap: 0.75rem;
            background: #edf5fc;
            border-radius: 12px;
            padding: 0.9rem 1rem;
            border: 2px solid #C8C0B5;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .info-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 18px rgba(18,18,18,0.08);
        }

        .info-item .icon {
            font-size: 1.2rem;
            color: #96281B; 
        }
        
        .map-placeholder {
            width: 100%;
            height: 250px;
            background: #ecf0f1;
            border: 1px solid #ccc;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #7f8c8d;
            margin-top: 1.5rem;
        }

        /* Popup overlay */
        .popup-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 9998;
            justify-content: center;
            align-items: center;
        }
        .popup-overlay.show {
            display: flex;
        }

        /* Popup box */
        .popup-box {
            background: white;
            border-radius: 16px;
            padding: 2.5rem 2rem;
            text-align: center;
            max-width: 380px;
            width: 90%;
            box-shadow: 0 8px 30px rgba(0,0,0,0.2);
            animation: popIn 0.3s ease;
        }

        @keyframes popIn {
            from { transform: scale(0.8); opacity: 0; }
            to   { transform: scale(1);   opacity: 1; }
        }

        .popup-icon {
            font-size: 3.5rem;
            margin-bottom: 1rem;
        }

        .popup-box h3 {
            font-size: 1.4rem;
            color: #121212;
            margin-bottom: 0.5rem;
        }

        .popup-box p {
            color: #555;
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
        }

        .popup-close-btn {
            background: #1A1A1A;
            color: white;
            border: none;
            padding: 0.7rem 2rem;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s ease;
        }
        .popup-close-btn:hover {
            background: #3A3A3A;
        }

        @media (min-width: 768px) {
            .contact-content {
                flex-direction: row; 
            }

            .contact-form, .contact-info {
                flex: 1;
            }
            
            .submit-button {
                width: auto;
            }
        }

    
/* ── PROFILE ── */
.profile-wrapper {
  position: relative;
  display: flex;
  align-items: center;
  gap: 0.6rem;
}
.profile-btn {
  background: #96281B;
  border: 2px solid rgba(255,255,255,0.7);
  color: white;
  border-radius: 50%;
  width: 40px;
  height: 40px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.2s;
  flex-shrink: 0;
  text-transform: uppercase;
}
.profile-btn:hover { background: #B03020; }
.profile-name {
  color: white;
  font-size: 0.95rem;
  font-weight: 600;
  white-space: nowrap;
}
.profile-dropdown {
  display: none;
  position: absolute;
  top: 50px;
  right: 0;
  background: white;
  border-radius: 8px;
  box-shadow: 0 4px 18px rgba(0,0,0,0.18);
  min-width: 170px;
  z-index: 999;
  overflow: hidden;
}
.profile-dropdown.open { display: block; }
.profile-dropdown a {
  display: block;
  padding: 0.7rem 1.2rem;
  color: #121212;
  text-decoration: none;
  font-size: 0.92rem;
  font-weight: 600;
  transition: background 0.15s;
  border-bottom: 1px solid #f0f0f0;
}
.profile-dropdown a:hover { background: #F7F5F2; }
.profile-dropdown .logout-btn {
  display: block;
  width: 100%;
  text-align: left;
  padding: 0.7rem 1.2rem;
  color: #96281B;
  background: none;
  border: none;
  font-size: 0.92rem;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.15s;
}
.profile-dropdown .logout-btn:hover { background: #fff0ef; }

</style>
</head>
<body>

    <header class="header">
        <div class="logo">SVAR<span style="color:#96281B;">A</span> </div>
        <nav class="nav">
            <a href="home1.html">Home</a>
            <a href="guitars1.html">Guitars</a>
            <a href="pianos1.html">Pianos</a>
            <a href="drums1.html">Drums</a>
            <a href="violins1.html">Violins</a>
            <a href="tutorials1.html">Tutorials</a>
            <a href="contact1.html" class="active" aria-current="page">Contact</a>
            <a href="about us1.html">About Us</a>
        </nav>
        <button class="mobile-menu">☰ Menu</button>
      <div class="profile-wrapper">
    <button class="profile-btn" id="profileBtn" onclick="toggleProfile()">&#128100;</button>
    <span class="profile-name" id="profileName"></span>
    <div class="profile-dropdown" id="profileDropdown">
      <a href="update.html">&#9999;&#65039; Update Info</a>
      <a href="de-register.html">&#10060; De-register</a>
      <button class="logout-btn" onclick="logOut()">&#128682; Log Out</button>
    </div>
  </div>
</header>

    <section class="contact-section">
        <h2 class="section-title">Get in Touch with the Music</h2>
        <p class="section-intro">Questions about instruments, orders, or lessons? Send us a message and we’ll help you out.</p>

        <div class="contact-content">
            <div class="contact-form">
                <h3>Send us a Message</h3>
                <form id="contactForm" action="contact.php" method="POST">
                    
                    <label for="name">Your Name</label>
                    <input type="text" id="name" name="name" required placeholder="Priya Yadav">
                    
                    <label for="email">Your Email</label>
                    <input type="email" id="email" name="email" required placeholder="name@example.com">
                    
                    <label for="subject">Subject</label>
                    <input type="text" id="subject" name="subject" required placeholder="Inquiry about Instruments">
                    
                    <label for="message">Message</label>
                    <textarea id="message" name="message" required placeholder="I have a question about..."></textarea>
                    
                    <button type="submit" class="submit-button">Submit Inquiry</button>
                </form>
            </div>

            <div class="contact-info">
                <h3>Our Details</h3>
                
                <div class="info-item">
                    <span class="icon">📍</span>
                    <p>123 Cyber music club, Music City, 421301</p>
                </div>
                
                <div class="info-item">
                    <span class="icon">📧</span>
                    <p>support@musicalworld.com</p>
                </div>
                
                <div class="info-item">
                    <span class="icon">📞</span>
                    <p>+91 7208XXXXXX</p>
                </div>
                
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="footer-links">
             <div><a href="https://www.instagram.com/svara_5137?igsh=MXZybDFlZG8yMjBrYw%3D%3D&utm_source=qr" style="color:white;" target="_blank">Follow Us</a></div>
        </div>
        <p>&copy; 2025 Musical Instruments World. All Rights Reserved.</p>
    </footer>

    <!-- Popup Modal -->
    <div class="popup-overlay" id="popupOverlay">
        <div class="popup-box">
            <div class="popup-icon">✅</div>
            <h3>Message Sent!</h3>
            <p>Thank you for reaching out. We'll get back to you as soon as possible.</p>
            <button class="popup-close-btn" onclick="closePopup()">OK</button>
        </div>
    </div>

    <script>
        // Show popup if redirected back from contact.php with ?status=success
        const params = new URLSearchParams(window.location.search);
        if (params.get('status') === 'success') {
            document.getElementById('popupOverlay').classList.add('show');
            // Clean the URL
            window.history.replaceState({}, document.title, window.location.pathname);
        }

        function closePopup() {
            document.getElementById('popupOverlay').classList.remove('show');
        }

        // Close popup if user clicks on the dark background
        document.getElementById('popupOverlay').addEventListener('click', function(e) {
            if (e.target === this) closePopup();
        });
    </script>

<script>
(function() {
  var name = localStorage.getItem('svara_user_name') || '';
  var btn = document.getElementById('profileBtn');
  var nameEl = document.getElementById('profileName');
  if (name) {
    btn.textContent = name.charAt(0).toUpperCase();
    nameEl.textContent = name;
  }
})();
function toggleProfile() {
  document.getElementById('profileDropdown').classList.toggle('open');
}
function logOut() {
  localStorage.removeItem('svara_user_name');
  localStorage.removeItem('svara_user_id');
  window.location.href = 'landing.html';
}
document.addEventListener('click', function(e) {
  var btn = document.getElementById('profileBtn');
  var dd = document.getElementById('profileDropdown');
  if (btn && dd && !btn.contains(e.target) && !dd.contains(e.target)) {
    dd.classList.remove('open');
  }
});
</script>
</body>
</html>
